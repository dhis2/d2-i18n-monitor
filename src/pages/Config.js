import React from 'react'
import Template from './Template'

export class Config extends React.Component {
  static path = '/config'

  render() {
    return (
      <Template>
        config
      </Template>
    )
  }
}
